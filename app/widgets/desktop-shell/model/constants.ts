/** How often the menu-bar clock re-renders, in ms. */
export const CLOCK_REFRESH_MS = 30_000

/** Characters of the label shown on a Dock icon glyph. */
export const DOCK_ICON_LABEL_LENGTH = 2

/** Wallpaper never fully disappears, even at progress 0. */
export const WALLPAPER_MIN_OPACITY = 0.15

/** Where a freshly opened window sits before the user drags it, in px. */
export const WINDOW_INITIAL_POSITION = { x: 120, y: 80 }

/** Chrome / window fade duration, in ms (mirrors the CSS transitions). */
export const CHROME_TRANSITION_MS = 400
