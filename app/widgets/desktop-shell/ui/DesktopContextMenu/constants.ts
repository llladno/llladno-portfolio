/** Fixed width of the context menu, in px. */
export const CONTEXT_MENU_WIDTH_PX = 212

/** Keep the menu at least this far from every viewport edge, in px. */
export const CONTEXT_MENU_EDGE_GAP_PX = 8

/** Rough height of one menu row, for keeping the menu on-screen, in px. */
export const CONTEXT_MENU_ROW_PX = 34
